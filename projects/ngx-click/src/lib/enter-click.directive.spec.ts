import { EnterClickDirective } from './enter-click.directive';

describe('EnterClickDirective', () => {
  it('should create an instance', () => {
    const directive = new EnterClickDirective();
    expect(directive).toBeTruthy();
  });
});
